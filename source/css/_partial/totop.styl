#totop
  position fixed
  bottom 50px
  right 50px
  cursor pointer
  &:before
      font-family font-icon-family
      font-smoothing()
      content "\f135"
  &:hover
    color color-theme
  transform scale(3) rotate(-45deg)
